# **UI 设计优化与功能新增建议**

## **一、 关于“持仓概况”模块的优化建议**

你感觉这个模块“有点怪”，主要原因是**视觉重心失衡**。左侧的走势图内容丰富、视觉重量大，而右侧的“持仓概况”卡片高度相同，但内容却非常稀疏，导致大量尴尬的留白。

### **1\. 问题诊断**

* **信息密度低**：在一个硕大的卡片里只放了“持有方式”和“持有份额”两行数据，空间利用率极低。  
* **排版割裂**：字段名（如“持有方式”）和数值（如“份额持有”）距离过远，视线阅读路径不流畅。  
* **视觉枯燥**：纯文字列表缺乏视觉锚点。

### **2\. 优化方案**

#### **增加关键数据**

既然占据了这么大的版面，应该展示更多维度的持仓数据，填补空白。

* **建议增加字段**：  
  * **持仓成本** (Cost Price)  
  * **累计收益** (Total Return) \- 不同于顶部的今日收益  
  * **持仓占比** (Position Ratio) \- 该基金占总资产的百分比  
  * **分红方式** (Dividend Mode) \- 现金分红/红利再投  
* **排版建议**：改为 **2x2 网格布局**，或者 **3行列表**，让数据铺满卡片。

## **二、 添加“实时走势图”的方案**

你想加一个实时走势（分时图），最好的方式是**不破坏现有布局**，而是利用**Tab切换**。

### **1\. 位置选择**

建议直接复用左下角的 净值走势 (30日) 卡片位置。

### **2\. 交互设计**

在卡片标题栏右侧添加 Tab 切换控件：

**\[ 分时 \]** \[ 近1月 \] \[ 近3月 \] \[ 近1年 \]

* **默认状态**：如果是交易时间，默认显示“分时”；如果是休市，默认显示“近1月”。  
* **分时图样式建议**：  
  * **基准线**：中间画一条虚线代表 0.00% (昨日收盘价)。  
  * **曲线颜色**：实时估值线建议使用**更鲜艳的实线**（如深绿色或红色，取决于涨跌），并带有渐变填充背景，增强实时感。  
  * **对比参照**：可以有一条浅灰色的线代表“大盘指数”或“同类平均”，作为对比。

## **三、 其他微调建议**

1. **左侧列表优化**：左侧列表中的选中状态（浅米色背景）很清晰，但建议给选中的卡片加一个极细的边框（例如深绿色），加强选中感。  
2. **顶部卡片对齐**：目前的四个顶部卡片（总资产、今日收益等）看起来高度很舒服，但字体大小层级可以再拉大一点（数字更大，标签更小），增强冲击力。  
3. **字体对齐**：右下角“持仓概况”里的数字字体似乎比左侧图表里的数字要大很多，建议统一下数字的字号规范。