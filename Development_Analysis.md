# AI 基金技术分析平台 - 深度开发分析文档

本中心文档旨在针对 [AI_Fund_Analysis_Platform_PRD.md](file:///c:/Users/spongzi/Documents/Code/JiJIn/AI_Fund_Analysis_Platform_PRD.md) 中提出的需求，进行详细的技术实现分析，重点解决**技术栈选型理由**、**API 高可靠性获取方案**以及**核心逻辑开发建议**。

---

## 1. 技术栈深度分析 (Tech Stack Deep Dive)

### 1.1 后端：FastAPI + Python 3.10
*   **选型理由**：
    *   **异步高性能**：基金实时估值需要高频请求多个个股接口，FastAPI 的 `asyncio` 支持能极大提升并发处理能力。
    *   **金融生态**：Python 是 `AkShare`、`Pandas`、`NumPy` 等金融量化库的母语，数据处理无缝衔接。
    *   **类型安全**：Pydantic 模型可确保金融数据的格式准确性，减少运行时错误。

### 1.2 前端：React + Tailwind CSS + Shadcn/UI
*   **选型理由**：
    *   **组件化**：适合构建复杂的看板，如“实时估值卡片”、“新闻影响流”、“K线图”等。
    *   **性能**：React 的虚拟 DOM 结合 `React-Query` 可以高效处理实时数据轮询刷新。
    *   **可视化**：使用 `ECharts` 或 `Lightweight Charts`（TradingView 开源库）实现专业的金融图表交互。

### 1.3 数据存储：DuckDB + SQLite
*   **选型理由**：
    *   **SQLite**：用于存储用户自选配置、系统设置等关系型轻量数据。
    *   **DuckDB**：作为“嵌入式分析型数据库”，它处理千万级历史新闻和净值数据的速度远超传统数据库，非常适合本地 AI 回测。

### 1.4 AI 引擎：Ollama + LangChain
*   **选型理由**：
    *   **完全本地化**：通过 Ollama 运行 `DeepSeek-R1` 或 `Llama-3`，确保投资组合和研究思路不外泄。
    *   **工作流编排**：LangChain 负责将“新闻文本”、“历史行情”和“机构动向”串联起来，喂给 AI 进行结构化输出。

---

## 2. API 可靠性获取方案 (Reliable Data Sourcing)

为确保平台在生产环境（即使是本地部署）中稳定运行，API 获取必须遵循“**多源冗余、降级处理、频率控制**”的原则。

### 2.1 基金与行情数据源 (Multi-Source Redundancy)
| 优先级 | 数据源 | 接口类型 | 特点 |
| :--- | :--- | :--- | :--- |
| **P0 (核心)** | **AkShare** | 库调用 | 整合了东方财富、同花顺、新浪等多方接口，最稳定。 |
| **P1 (实时)** | **Sina/Tencent API** | HTTP Get | 极低延迟，适合 5s 一次的个股实时刷新。 |
| **P2 (备份)** | **Tushare** | Token API | 接口稳定但有积分限制，适合作为历史数据补全。 |

### 2.2 提高可靠性的技术手段
1.  **负载均衡器 (Provider Switcher)**：
    *   后端实现一个 `DataFetcher` 类，当新浪接口请求失败或超时（如 2s 无响应）时，自动切换至腾讯接口。
2.  **本地缓存层 (Local Cache)**：
    *   **静态数据**：基金名称、代码、经理信息等，每周更新一次并持久化。
    *   **半动态数据**：重仓持股列表，每季度更新后存储在 SQLite，避免交易时间内频繁调用。
3.  **频率与反爬策略 (Anti-Ban)**：
    *   实现 `Token Bucket`（令牌桶）算法控制请求速率。
    *   使用随机 `User-Agent` 模拟真实浏览器行为。
4.  **断点续传与补偿**：
    *   每日 20:00 后的净值更新任务若失败，系统需记录状态并在下次启动时自动补抓缺失日期。

---

## 3. 关键功能开发逻辑分析

### 3.1 实时估值计算逻辑 (Real-time Valuation)
1.  **初始化**：读取本地 SQLite 中存储的该基金 Top 10 持仓及权重。
2.  **并发请求**：使用 `httpx` 同时请求 10 只个股的实时价格。
3.  **加权计算**：
    `估值涨跌 = Σ (个股实时涨幅 * 持仓占比) / Σ (已知持仓总占比)`
4.  **修正因子**：计算“其他仓位”的平均波动（参考对应行业指数），以减小误差。

### 3.2 AI 新闻价值分析逻辑 (AI News Analysis)
1.  **清洗**：利用正则表达式剔除广告和重复资讯。
2.  **向量检索 (RAG)**：
    *   将新新闻转化为向量，在 DuckDB 中检索历史 3 年内相似的新闻。
    *   提取这些历史新闻发生后的 3 日股价走势。
3.  **Prompt Engineering**：
    > "作为金融分析专家，对比以下历史案例：[历史案例A/B]，分析当前新闻 [当前新闻内容] 对 [相关板块] 的潜在影响。请给出实质性评分（0-10）并说明理由。"

---

## 4. 本地部署实施路径 (Implementation Path)

1.  **第一阶段：基础设施**
    *   搭建 FastAPI 基础框架，集成 AkShare 获取基础基金列表。
2.  **第二阶段：实时行情流**
    *   开发个股实时获取模块，实现基于持仓权重的加权估值算法。
3.  **第三阶段：AI 实验室**
    *   配置 Ollama 环境，编写 LangChain 链，实现新闻的自动化结构化处理。
4.  **第四阶段：前端集成**
    *   使用 Shadcn/UI 构建仪表盘，接入 WebSocket 或长轮询展示实时估值动态。

---

## 5. 风险防范 (Risk Mitigation)

*   **API 变更风险**：金融数据源经常更改接口路径。
    *   *对策*：后端采用插件化设计，数据抓取逻辑与业务逻辑解耦，只需修改特定的 `scraper` 脚本即可修复。
*   **计算性能风险**：如果用户自选基金过多（>100只），并发压力大。
    *   *对策*：引入任务队列（如 `TaskIQ` 或简单的后台任务），分批次刷新，并优先刷新当前屏幕可见的基金。

---
**文档结论**：
本项目在技术上完全可行。核心难点不在于界面，而在于**数据清洗的准确性**与**AI 分析的逻辑严密性**。建议初期优先保证“实时估值”的准确度，再逐步迭代 AI 分析功能。
