# AI 基金技术分析平台 - 开发指南 (Development Guide)

本指南基于 [Development_Analysis.md](file:///c:/Users/spongzi/Documents/Code/JiJIn/Development_Analysis.md)，为开发者提供分阶段的实施建议。您可以根据自身的技术储备和时间安排，选择合适的开发计划。

---

## 🚀 Plan 1: 核心估值版 (MVP - 最小可行性产品)
**目标**：实现最核心的“自选基金实时估值”与“真实净值对账”功能，确保数据跑通。

### 1. 开发重点
*   **基础环境**：搭建 FastAPI + SQLite。
*   **数据采集**：集成 `AkShare` 获取基金基本信息及重仓股（每季更新）。
*   **实时引擎**：实现 5s 轮询个股行情并计算加权涨跌幅。
*   **基础 UI**：使用 React + ECharts 展示简单的涨跌幅列表和估值折线。

### 2. 关键路径
1.  **基金库初始化**：实现从代码搜索基金并持久化到本地 SQLite。
2.  **估值逻辑实现**：编写加权平均算法，处理停牌、缺少仓位等边缘情况。
3.  **定时对账任务**：每日 20:30 自动抓取真实净值，计算误差。

---

## 🤖 Plan 2: AI 增强版 (AI-Driven Insights)
**目标**：引入 AI 引擎，实现自动化新闻解读与历史影响回测。

### 1. 开发重点
*   **本地 AI 部署**：安装 `Ollama` 并运行 `DeepSeek-R1` 或 `Llama-3`。
*   **新闻流抓取**：实现财联社或东财新闻的实时流式抓取。
*   **RAG (检索增强生成)**：
    *   将历史新闻存入 `DuckDB`。
    *   AI 分析当前新闻时，自动匹配历史相似事件并输出回测胜率。
*   **情绪指标**：AI 根据新闻内容生成“市场情绪温度计”。

### 2. 关键路径
1.  **Prompt 优化**：针对金融语境调优 AI 的提取和总结能力。
2.  **向量数据库构建**：实现新闻文本的 Embedding 存储与相似度检索。
3.  **前端交互升级**：在基金看板旁增加“AI 深度解读”侧边栏。

---

## 🏆 Plan 3: 全功能专业版 (Professional Trader Suite)
**目标**：构建具备“机构动向监控”与“高级策略回测”能力的专业看板。

### 1. 开发重点
*   **机构筹码分析**：
    *   监控龙虎榜、北向资金流向。
    *   分析大宗交易数据，识别机构“进场”信号。
*   **高性能架构**：
    *   引入 `Redis` 缓存极高频数据。
    *   前端采用 `WebSocket` 实现数据的真正“零延迟”推送。
*   **专业可视化**：接入 `TradingView` 的 Lightweight Charts，支持在图表上标注 AI 事件点和机构进场点。

### 2. 关键路径
1.  **数据冗余体系**：实现自动多源切换（Sina/Tencent/Eastmoney），确保极端市场下数据不中断。
2.  **公募仓位预测模型**：基于行业指数与基金净值的相关性，实时推算基金经理的加减仓动态。
3.  **本地部署优化**：提供 Docker 一键部署脚本，整合所有依赖环境。

---

## 🛠 开发建议汇总

| 维度 | Plan 1 (MVP) | Plan 2 (AI+) | Plan 3 (Pro) |
| :--- | :--- | :--- | :--- |
| **开发周期** | 1-2 周 | 3-5 周 | 8 周+ |
| **核心难点** | 数据清洗与算法准确性 | AI 模型 Prompt 调优 | 高并发与多源数据融合 |
| **推荐技术** | Python + SQLite | LangChain + Ollama | DuckDB + WebSocket |
| **用户价值** | 知道亏了多少 | 知道为什么亏/赚 | 知道下一步该怎么走 |

---

**文档版本**: 1.0  
**分析日期**: 2026-02-07  
**状态**: 建议从 **Plan 1** 开始迭代，在确保数据准确的基础上，再引入 AI 模块。
